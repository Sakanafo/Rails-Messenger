<%== pagy_bootstrap_nav @pagy %>
  <% @msg.each do |message| %>
    <div class="toast fade show" role="alert" aria-live="assertive" aria-atomic="true">
      <div class="toast-header">
        <strong class="mr-auto">UserName</strong>
        <small><time datetime="<%= message.formatted_created_at %>">
          <%= message.formatted_created_at %></time></small>

        <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
          <span aria-hidden="true">
            <%= link_to image_tag("delete.png", alt: "[x]", size: "10x10"), message_path(message), data: {"turbo-method": :delete, confirm: 'Are you sure?'} %>
          </span>
        </button>
      </div>

        <div class="toast-body">
          <p class="text-break">
          <%= message.body %></p>
        </div>
    </div>
  <% end %>

<%= render 'form'%>
